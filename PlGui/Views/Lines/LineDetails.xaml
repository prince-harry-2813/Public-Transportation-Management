<UserControl
    x:Class="PlGui.Views.Lines.LineDetails"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fa5="http://schemas.fontawesome.com/icons/"
    xmlns:local="clr-namespace:PlGui.Views.Lines"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <StackPanel
        Width="580"
        Margin="10"
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
        <WrapPanel Margin="10" HorizontalAlignment="Center">
            <fa5:ImageAwesome Height="14pt" Icon="Solid_BusAlt" />
            <Label FontSize="14pt" FontWeight="SemiBold">
                פרטי הקו
            </Label>
        </WrapPanel>

        <WrapPanel Margin="10">
            <!--  Edit Bus Button  -->
            <StackPanel HorizontalAlignment="Center" Visibility="Visible">
                <Button
                    Name="TreatmentButton"
                    Height="30pt"
                    Margin="20,5"
                    Background="#00DDDDDD"
                    BorderBrush="#00707070"
                    Command="{Binding LineDetailsButtonCommand}"
                    CommandParameter="Edit"
                    ToolTip="Edit BusStop Properties">
                    <Button.Content>
                        <fa5:ImageAwesome
                            Margin="2"
                            Foreground="Blue"
                            Icon="Regular_Edit" />
                    </Button.Content>
                </Button>

                <Label
                    Padding="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    VerticalContentAlignment="Top"
                    FontSize="10pt"
                    Foreground="Blue">
                    Edit
                </Label>
            </StackPanel>

            <!--  Delete Bus Button  -->
            <StackPanel HorizontalAlignment="Center" Visibility="Visible">
                <Button
                    x:Name="RefuleButton"
                    Height="30pt"
                    Margin="5"
                    Background="#00DDDDDD"
                    BorderBrush="#00707070"
                    Command="{Binding LineDetailsButtonCommand}"
                    ToolTip="Delete BusStop">
                    <Button.Content>
                        <fa5:ImageAwesome
                            Margin="2"
                            Foreground="Blue"
                            Icon="Solid_Trash" />
                    </Button.Content>
                </Button>

                <Label
                    Padding="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    VerticalContentAlignment="Top"
                    FontSize="10pt"
                    Foreground="Blue">
                    Delete
                </Label>
            </StackPanel>
        </WrapPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>


            <!--  Bus properties Detais  -->
            <!--<ListBox
                x:Name="lbLineDetails"
                Grid.Column="0"
                Margin="10"
                HorizontalContentAlignment="Stretch"
                ItemsSource="{Binding LbItemSource}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="20,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>

                            <Label
                                MinWidth="150"
                                HorizontalAlignment="Left"
                                Content="{Binding PropertyName, Mode=TwoWay}"
                                FontSize="8pt"
                                FontWeight="Heavy" />

                            <TextBox
                                Grid.Column="1"
                                MinWidth="150"
                                HorizontalAlignment="Left"
                                BorderBrush="Black"
                                BorderThickness="0.5"
                                FontSize="8pt"
                                IsReadOnly="True"
                                Text="{Binding PropertyValue , Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>-->

            <StackPanel
                Margin="10"
                Background="White"
                DataContext="{Binding Line , Mode=TwoWay}"
                IsEnabled="{Binding IsInEditMode}">
                <!--  ID  -->
                <DockPanel HorizontalAlignment="Stretch">
                    <Label
                        Margin="10"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Stretch"
                        FontSize="8pt">
                        ID : 
                    </Label>
                    <TextBox
                        Margin="10"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        BorderBrush="Black"
                        BorderThickness="1"
                        Text="{Binding Id , Mode=TwoWay}" />
                </DockPanel>
                <!--  Code  -->
                <DockPanel HorizontalAlignment="Stretch">
                    <Label
                        Margin="10"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        FontSize="8pt">
                        Code :
                    </Label>
                    <TextBox
                        Margin="10"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        BorderBrush="Black"
                        BorderThickness="1"
                        Text="{Binding Code ,Mode=TwoWay}" />
                </DockPanel>
                <!--  Area  -->
                <DockPanel HorizontalAlignment="Stretch">
                    <Label
                        Margin="10"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        FontSize="8pt">
                        Area : 
                    </Label>
                    <TextBox
                        Margin="10"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        BorderBrush="Black"
                        BorderThickness="1"
                        Text="{Binding Area ,Mode=TwoWay}" />
                </DockPanel>
                <!--  FirstStation  -->
                <DockPanel HorizontalAlignment="Stretch">
                    <Label
                        Margin="10"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        FontSize="8pt">
                        First Station :
                    </Label>
                    <TextBox
                        Margin="10"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        BorderBrush="Black"
                        BorderThickness="1"
                        Text="{Binding FirstStation.Station.Name , Mode=TwoWay}" />
                </DockPanel>
                <!--  LastStation  -->
                <DockPanel HorizontalAlignment="Stretch">
                    <Label
                        Margin="10"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        FontSize="8pt">
                        LastStation :
                    </Label>
                    <TextBox
                        Margin="10"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        BorderBrush="Black"
                        BorderThickness="1"
                        Text="{Binding LastStation.Station.Name , Mode=TwoWay}" />
                </DockPanel>
            </StackPanel>

            <!--  Stops of line Details  -->
            <ScrollViewer Grid.Column="1">
                <ItemsControl
                    Margin="10"
                    HorizontalContentAlignment="Stretch"
                    Background="White"
                    ItemsSource="{Binding Line.Stations , Mode=TwoWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Stretch" FontSize="8pt"
                                       Margin="10">
                                <Run
                                    Text="{Binding Station.Name , Mode=TwoWay}" />
                                <Run Text="{Binding Station.Code , Mode=TwoWay}" />
                            </TextBlock>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

        </Grid>
    </StackPanel>

</UserControl>
